# GEMINI.md — Gemini CLI Agent Config

@ag3nts.md

## Gemini-Specific Notes

- This config is managed via the Terminal-AI portable SSD setup.
- Global config lives in `shared/gemini-cli/`, platform config in `<platform>/gemini-cli/config/`.
- Authenticate via `gemini` command on first run (opens browser for Google OAuth).
- When editing Terminal-AI setup scripts, always verify with the setup command after changes.

# CLAUDE.md — Claude Code Agent Config

@ag3nts.md

## Claude-Specific Notes

- This config is managed via the Terminal-AI portable SSD setup.
- Global config lives in `shared/claude-code/`, platform config in `<platform>/claude-code/config/`.
- Use `/compact` when context usage exceeds 60%.
- Use `/init` in new project directories to generate project-level CLAUDE.md files.
- When editing Terminal-AI setup scripts, always verify with the setup command after changes.

# AGENTS.md — Codex CLI Agent Config

@ag3nts.md

## Codex-Specific Notes

- This config is managed via the ag3nts portable SSD setup.
- Global config lives in `shared/codex-cli/`, platform config in `<platform>/codex-cli/config/`.
- Codex config file: `config.toml` in the codex config directory.
- Authenticate via `codex` command on first run (opens browser for ChatGPT OAuth).
- When editing ag3nts setup scripts, always verify with the setup command after changes.
